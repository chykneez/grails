import React from 'react';

import { LoaderOverlay, LoaderContainer } from './LoaderStyles';

const Loader = () => (
  <LoaderOverlay>
    <LoaderContainer />
  </LoaderOverlay>
);

export default Loader;
